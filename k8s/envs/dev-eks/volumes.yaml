apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama
  namespace: phrasify
  labels:
    app: ollama

spec:
  accessModes:
    - ReadWriteMany
  storageClassName: efs-sc
  resources:
    requests:
      storage: 8Gi
